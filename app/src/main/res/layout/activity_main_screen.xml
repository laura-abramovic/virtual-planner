<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activities.visuals.MainScreenActivity"
    android:background="@color/background_main">

    <RelativeLayout
        android:id="@+id/userDataDisplayId"
        android:layout_width="match_parent"
        android:layout_height="80dp">

        <TextView
            android:id="@+id/usernameDisplayId"
            android:text="@string/empty_string"
            android:textColor="@color/text_main"
            android:textSize="30dp"
            android:layout_marginLeft="40dp"
            android:layout_marginTop="25dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

        <ImageView
            android:src="@drawable/ic_baseline_menu"
            android:layout_alignParentRight="true"
            android:layout_marginRight="20dp"
            android:layout_marginTop="20dp"
            android:layout_width="50dp"
            android:layout_height="50dp" />

    </RelativeLayout>

    <View
        android:id="@+id/lineSeparatorId"
        android:layout_below="@id/userDataDisplayId"
        android:background="@color/text_main"
        android:layout_marginHorizontal="10dp"
        android:layout_width="match_parent"
        android:layout_height="2dp" />

    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_above="@id/bottom_navigation"
        android:layout_below="@id/lineSeparatorId"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        app:menu="@menu/bottom_navigation"
        android:background="?android:attr/windowBackground"/>

</RelativeLayout>