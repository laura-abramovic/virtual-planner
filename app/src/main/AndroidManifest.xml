<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.virtualplanner">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VirtualPlanner">
        <activity android:name=".entry.RegisterActivity"></activity>
        <activity android:name=".entry.LoginActivity" />
        <activity
            android:name=".visuals.ToDoListActivity"
            android:label="@string/title_activity_to_do_list"
            android:theme="@style/Theme.VirtualPlanner.NoActionBar" />
        <activity android:name=".visuals.NotesActivity" />
        <activity android:name=".visuals.DailyActivity" />
        <activity android:name=".visuals.CalendarActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Design.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>